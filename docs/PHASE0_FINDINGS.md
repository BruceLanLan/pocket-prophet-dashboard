# Phase 0 观测记录

对应 `docs/PLAN.md` Phase 6（`docs/PLAN-v2.md`）的补做。测试图：四角黑色定位方块
+ 四级灰阶色块横条 + 大号时间戳 `21:54:12` + "PHASE 0 TEST" + 日期，2026-07-29 推送。

## 问题 1：停驻在"更换壁纸"界面时，屏幕显示的是什么？

**结果：显示的就是推送的壁纸内容本身**（用户目视确认与 `COMPRESS_RENDER` 一致），不是二维码或配置提示界面。

**这是本项目迄今最重要的一条确认**：结合已知的"停驻该界面可连续在线 60+ 分钟"（探测 A，`docs/evidence/poll-A-while-page-open.log`），意味着——

**停驻模式成立**：只要设备保持停在"更换壁纸"界面不退出，它在这段时间内既联网可推送，屏幕又确实显示壁纸内容。定时轮换推送（`docs/PLAN-v2.md` Phase 7）在此基础上是可行的，不需要依赖"离网后内容是否持久"这个更弱的条件。

对应 `docs/PLAN-v2.md` Phase 6 的分支判定：**结果 A**——Phase 7 全量做（停驻模式 + 定时轮换推送）。

## 问题 2、3：退出界面后图还在不在、会不会被原生内容覆盖

**用户决定不测**（2026-07-29）。理由：这两条只影响"推送后退出界面"这种用法的体验，而 Phase 7 采用的停驻模式设计上根本不会退出界面，跟这两个问题无关。若日后需要支持"推送后退出仍要看效果"这种用法，这两条需要补测。

## 问题 4：从 POST 到画面实际变化经过多久？

推送发起于 `21:54:12`（本地时间戳，非设备显示时刻），用户确认时画面已是最终内容，未观测到明显延迟。
